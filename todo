Runner의 run 메서드가 이미 데이터가 있는 경우는 해당 메서드 실행하지 않도록 하기
connection refused 에러 해결하기
 먼저 간단히 구현해보기
  API를 분당 15회 호출하고, 이를 두 번 실행한다
 에러 원인: 웹페이지 html 요청 API를 분당 15회만 호출할 수 있다
 해결 방안: API 호출을 분당 15회 호출
 구현 어떻게?
  데이터가 109개이므로, 총 8회 호출해야 한다. 따라서 8분 걸린다.
  데이터를 순서대로 15개씩 가져올 수 있어야 한다
   한 번 가져온 다음 가져올 때는 이전에 가져온 데이터는 건너뛰고 가져와야 한다
  15번 요청하고 나면 1분 후에 다음 15번의 요청할 수 있어야 한다
  API rate limit이 사용자 한 명에 대한 것이므로, 여러 개의 스레드를 이용할 수 없을 것 같다
  매 15번의 API 호출한 응답 데이터를 어딘가에 저장해두고, 여기에 계속해서 연산 결과를 덧대야 한다

 왜 실패할까?
  매 15개마다 마지막 15번째 url에 대한 curl 호출 후 2분을 쉬게 했는데도 connect refused가 발생한다
  1분은 어떤 걸 기준으로 해야 하나? 마지막 url에 대한 curl 호출 후 응답받는 시점? 아니면 curl을 호출한 시점?
 실행 흐름이 어떻게 되어야 할까?
  각 연/월에 작성한 블로그 글 목록을 보여주는 url을 입력받는다
  위 url 목록을 리스트에 넣는다
  분당 일정한 갯수의 url을 받아서 그 url을 입력으로 아래 block의 연산을 실행한다
   block: 각 url에 대해
           curl로 url을 호출해 html 파일을 가져온다
           가져온 html에서 블로그 글의 제목과 그 글의 하이퍼링크값을 가져온다
           이 두 정보를 post 객체로 만들어, post 리스트에 추가한다
   1분 쉰다
    마지막은 쉬지 않는다

코드 정리하기
 테스트하지 않는 메서드 테스트하기
wayback machine api 요청 결과 중 timestamp가 바뀌는지 알아보기
 만약 바뀌지 않는다면, 매번 새로 인덱스 페이지를 만들 필요 없다
